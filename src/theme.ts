import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#4682b4", // mainBgColor
      light: "#6fa1cc", // header
      dark: "#2a4b6d", // weatherCard
    },
  },
});
